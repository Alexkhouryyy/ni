{
  "name": "jarvis-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174 --host",
    "build": "tsc && vite build",
    "preview": "vite preview --port 5174 --host"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
